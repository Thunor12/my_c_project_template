// Default app — no optional dependencies required.
#include <stdio.h>

int main(void)
{
    printf("c_project_template: build OK\n");
    printf("Deps catalog: config/features/ — enable with scripts/enable-*.sh\n");
    printf("Examples: ./nob examples (hello + optional demos)\n");
    return 0;
}
